# frozen_string_literal: true
source 'https://rubygems.org'

# The gem's dependencies are specified in the gemspec
gemspec

gem 'rake'
gem 'coveralls', require: false
gem 'activesupport', '<5' # so I can test against Ruby 2.1
gem 'rubocop-rspec', '<1.5' # so I can test against Ruby 2.1
gem 'json', '>2' # So I can test against Ruby 2.4
